# OpenSpec Configuration — web-emergentes
# Auto-generated by sdd-init on 2026-05-31

project: web-emergentes
stack:
  runtime: browser
  language: TypeScript 5.9
  framework: React 19
  bundler: Vite 7
  state: Zustand 5
  rendering:
    - Three.js 0.184 (3D arena)
    - Phaser 3.90 (battle board overlay)
  animation: GSAP 3.15
  data: PapaParse 5.5 (CSV catalog)

context: >
  Pokémon TCG battle demo with offline playable loop. Feature-based
  architecture (src/features/{battle,cards,npc,catalog}), pure functions
  for game engine, Zustand store for state, NpcService interface with
  mock/HTTP adapter, CSV card catalog, 3D arena + Phaser overlay.
  UI labels in Spanish. No CI/CD, no linter, no formatter configured.

strict_tdd: true

testing:
  runner: vitest
  command: npm test
  layers:
    unit: vitest
    integration: null
    e2e: null
  coverage: false
  type_checker: tsc (strict mode in tsconfig.app.json)
  linter: null
  formatter: null
  test_count: 12
  test_files:
    - src/features/battle/gameEngine.test.ts
    - src/features/battle/store.test.ts
    - src/features/cards/cardRepository.test.ts
    - src/features/npc/httpNpcService.test.ts

phases:
  proposal: openspec/changes/{change-id}/proposal.md
  spec: openspec/changes/{change-id}/spec.md
  design: openspec/changes/{change-id}/design.md
  tasks: openspec/changes/{change-id}/tasks.md
  apply: (in-code implementation)
  verify: openspec/changes/{change-id}/verify.md
  archive: openspec/changes/archive/{change-id}.md

conventions:
  features: src/features/{domain}/
  game_engine: Pure functions in gameEngine.ts, store in store.ts
  testing: Co-located *.test.ts alongside source
  ui_language: Spanish (UI labels, logs)
  csv_source: public/data/pokemon_cards_gen1_img.csv
  audio: public/audio/{music,sfx}/
